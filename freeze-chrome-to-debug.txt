>> setTimeout(function () {
  debugger;
}, 5000);

   run the snippet
   
   click on the ui which shows content on may be hovering over it
   
   once this snippet executes browser will open up debugger
   but the hovering component will still be present and therefore
   
   can inspect its elements
